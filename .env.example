# 复制本文件为 .env 并填入真实密钥（.env 已被 .gitignore 排除，不会提交）
# 字段名需与 application.yml 中 ${oc.env:VAR} 引用的变量名一致

# 阿里云 DashScope（qwen-max / deepseek-v3.2 等）
DASHSCOPE_API_KEY=sk-your-dashscope-key

# openai-proxy（glm-5.1 等）
OPENAI_PROXY_API_KEY=sk-your-openai-proxy-key
