### 🕐 基础环境信息
{datetime}

{db_metadata}

### 💬 用户问题
{question}

---

### 🗄️ 可用数据表与字段

{table_column_list}

### 📊 可参考指标定义

{metric_list}

---

### 📝 生成前必须完成的自检步骤

在输出 HQL 之前，请在内部完成以下核查（无需输出核查过程）：

1. 列出本次查询需要用到的所有表名 → 逐一确认每个表名出现在【可用数据表与字段】中
2. 如果核心表**完全不存在**于上下文中 → 停止生成，输出 `UNABLE_TO_ANSWER`
3. 如果仅部分字段暂时缺失，但核心表存在 → 继续生成 HQL，尽量使用现有字段，后续补全节点会处理缺失字段

只有核心表完全缺失时才拒绝回答，字段缺失不应阻止 HQL 生成。