## 🕐 基础环境信息
{datetime}

{db_metadata}

---

## 💬 用户意图
- **原始查询**: `{question}`

---

## ⚠️ 核心校验信息（本轮必须修复的问题）

> **以下是校验节点发现的错误，请自主分析根因并针对性修复：**

{validates}
>-
---

## 📝 当前待修复 HQL

```sql
{hql}
```
>-
---

## 📜 历史修复轨迹

> 每轮格式为：`第N轮：针对以下校验问题尝试修复` → 错误清单 → `修复后的 HQL：<HQL内容>`，各轮之间以 `---` 分隔。
> 若为空则代表这是第一轮修复；若不为空，**必须逐轮对比**当前待输出的 HQL 是否与任意一轮的 `修复后的 HQL` 在逻辑上完全一致。

{fix_history}
---

## 🚫 数据源中确认不存在的字段/指标

> 以下字段或指标经过多轮补全尝试，在元数据中**完全查不到**，说明它们**根本不存在于当前数据源**：

{unfound_fields}

> ⚠️ **严禁**在生成的 HQL 中引用上述任何字段或指标。若用户意图依赖这些字段，**必须改用数据表字段信息中真实存在的字段**替代。

---

## 📚 业务元数据

### 🗄️ 可用数据表与字段

{table_column_list}

> 🚨 **以上就是全部可用表。如果你的 HQL 中 FROM/JOIN 引用了上述列表之外的任何表名，该 HQL 必定报错 `Table not found`。请在输出前逐一核对。**

---

### 📊 可参考指标定义

{metric_list}

## ⛔ 最终警告：死循环是修复彻底失败的信号

> 在输出 HQL 之前，**必须最后检查两件事**：
>
> 1. 当前输出的 HQL 是否与历史修复轨迹中**任意一轮**在逻辑上完全一致？若是，立即停止，切换策略。
> 2. FROM/JOIN 中的每个表名是否都在「可用数据表与字段」中？若有任何一个不在其中，立即重写。